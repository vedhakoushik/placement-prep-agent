# Test dependencies — pinned to exact versions that work locally.
# No API keys needed — all external calls are mocked.

pytest>=7.0
pydantic>=2.0

# LangGraph core + SQLite checkpointer
# (SqliteSaver moved to a separate package in LangGraph 0.2+)
langgraph==1.2.1
langgraph-checkpoint-sqlite==3.1.0

# Required for node function imports in day38 (lazy imports, always mocked)
tavily-python==0.7.24
google-generativeai==0.7.2
